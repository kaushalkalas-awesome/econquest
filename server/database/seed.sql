-- EconQuest seed data is loaded via Node for maintainability (JSON lessons, bcrypt hashes).
-- After applying schema.sql, run from the `server` folder:
--
--   npm run seed
--
-- This truncates tables and inserts achievements, shop items, 6 quests (lessons + challenges), and demo users.

SELECT 'Run: cd server && npm run seed' AS instruction;
